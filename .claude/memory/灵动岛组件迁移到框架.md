---
name: ""
description: 把 reef-ui 完整视觉能力补入 reef-widgets，让 dynamic_island 切到统一 Widget 渲染范式。5 阶段已完成，需继续画绘制器适配和动画帧驱动。
metadata: 
  node_type: memory
  type: project
  originSessionId: be257ce5-9976-4909-81d2-8ebf966e715f
---

灵动岛组件迁移到 reef-widgets 框架的任务进度。

**Why**: 当前存在两套独立的灵动岛渲染体系——reef-widgets 有骨架但视觉保真度低，dynamic_island + reef-ui 功能完整但未框架化。需要统一。

**How to apply**: 继续 phase 5 的剩余工作：Windows 绘制器适配 VisualPrimitive、动画帧驱动切换到 WidgetHost。

## 已完成（5 阶段）

1. **Card 视觉规格** - 8 种样式完整配色系统、BodyRole/Badge/ToolPill/SettingsRow，颜色与 reef-ui 对齐
2. **CompactBar + Chrome 过渡** - 肩部集成、跑马灯、操作按钮、发光叠加、ChromeVisibility 过渡状态
3. **吉祥物完整功能** - mascot_bubble/badge/sprite 新模块，MessageBubble/WakeAngry 姿态，阴影和运动系统
4. **动画引擎 + 交互层** - animation.rs（缓动/交错揭示）、interaction.rs（指针区域/命中测试）
5. **桥接 dynamic_island** - island_widget_bridge.rs（RuntimeSnapshot→IslandWidget），dynamic_island 添加 reef-widgets 依赖

## 待完成（继续 Phase 5）

1. Windows 绘制器适配 - 增加消费 VisualPrimitive 的路径（当前消费 NativePanelRenderCommandBundle）
2. 动画帧驱动 - NativePanelAnimationFrameScheduler 更新 IslandWidget 字段 → WidgetHost::mark_dirty() + render()
3. 逐步精简 native_panel_renderer/mod.rs facade
4. 清理不再需要的模块引用

## 关键文件

- `framework/reef-widgets/src/island_widget.rs` - 顶层 Widget，Chrome 过渡 + 卡片动画
- `framework/reef-widgets/src/card.rs` - 完整视觉规格
- `framework/reef-widgets/src/compact_bar.rs` - ChromeVisibility + 跑马灯 + 操作按钮
- `framework/reef-widgets/src/animation.rs` - 动画系统
- `framework/reef-widgets/src/interaction.rs` - 交互层
- `dynamic_island/src/island_widget_bridge.rs` - 运行时快照 → Widget 树
- `dynamic_island/src/native_panel_renderer/mod.rs` - 需精简的 facade
